# Cấu hình kết nối cơ sở dữ liệu (Hỗ trợ biến môi trường cho Production)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/website_application?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:ngocminh2609}

# Hibernate cấu hình
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Port mặc định cho ứng dụng (Render/Railway sẽ tự động cấp port qua biến PORT)
server.port=${PORT:8080}

# URL public cua BE (dung de tao proxy URL cho anh, phai khop voi domain tren Render)
app.base-url=${APP_BASE_URL:http://localhost:8080}

# Cấu hình Google Login
google.client.id=${GOOGLE_CLIENT_ID:1051116450325-qneacpielnd6acgajc3kftfpk9nkjkqj.apps.googleusercontent.com}

# Local dùng MinIO (localhost:9000), production dùng Backblaze B2 thay thế qua biến môi trường
minio.endpoint=${MINIO_ENDPOINT:http://localhost:9000}
minio.access-key=${MINIO_ACCESS_KEY:admin}
minio.secret-key=${MINIO_SECRET_KEY:12345678}
minio.bucket.product=${MINIO_BUCKET_PRODUCT:products-image}
minio.bucket.category=${MINIO_BUCKET_CATEGORY:products-image}
minio.bucket.user=${MINIO_BUCKET_USER:products-image}
minio.bucket.banner=${MINIO_BUCKET_BANNER:products-image}
minio.url.external=${MINIO_EXTERNAL_URL:http://localhost:9000}
minio.region=${MINIO_REGION:us-east-1}

# Cấu hình VNPAY
vnp.tmnCode=${VNP_TMN_CODE:S5HW1QFO}
vnp.hashSecret=${VNP_HASH_SECRET:UD6YYMZLTBXWY9ACM60E15355P6JIUQH}
vnp.payUrl=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnp.returnUrl=${VNP_RETURN_URL:http://localhost:5173/payment-success}

# Giới hạn kích thước file upload (mặc định Spring Boot chỉ cho 1MB nên ảnh lớn bị từ chối)
spring.servlet.multipart.max-file-size=4MB
spring.servlet.multipart.max-request-size=4MB
