import type { CSSProperties } from "react";

export const styles = {
  content: {
    padding: "40px 20px",
  } as CSSProperties,
  headerContainer: {
    marginBottom: "40px",
  } as CSSProperties,
  title: {
    color: "var(--text-main)",
  } as CSSProperties,
  subtitle: {
    color: "var(--text-muted)",
  } as CSSProperties,
  loadingContainer: {
    textAlign: "center",
    padding: "100px",
  } as CSSProperties,
  emptyCard: {
    textAlign: "center",
    padding: "50px",
  } as CSSProperties,
  emptyIcon: {
    fontSize: 64,
    color: "var(--primary-color)",
  } as CSSProperties,
  emptyText: {
    color: "var(--text-muted)",
  } as CSSProperties,
  itemName: {
    fontSize: 13,
  } as CSSProperties,
  totalAmountText: {
    color: "#ee636e",
  } as CSSProperties,
};
